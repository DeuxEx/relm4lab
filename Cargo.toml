[package]
name = "relm4-test"
version = "0.0.1"
edition = "2024"

[dependencies]
# Vi låter relm4 hantera gtk4-beroendet automatiskt
relm4 = "0.9"
# used for api communication over the dbus protocol
zbus = "4.0" # or the latest version
tokio = { version = "1.0", features = ["full"] }
